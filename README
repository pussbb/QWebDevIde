QWebDevIde attempt to make own lightweight development environment

Screenshorts http://images.krabik.co.ua/albums/gl/2/qwebide - some of them can be old

lang files
for Linux users if application couldn't get available languages
create a symbol link to the run
cd ./bin
ln -s ../lang ./lang

For Windows user copy folder lang to folder bin